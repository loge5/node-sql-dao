const DatabaseAccessObject = require('./DatabaseAccessObject')

describe('DatabaseAccessObject', () => {
  it('should be defined', () => {
    expect(typeof DatabaseAccessObject).toBe('function')
  })
})
